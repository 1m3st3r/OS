Задача:
Система очередей с несколькими производителями и потребителямиРеализуйте многопоточную систему очередей, в которой несколько
потоков добавляют элементы в очередь (производители), а другиепотоки извлекают элементы (потребители). Необходимо обеспечить
корректный доступ к очереди, чтобы избежать ситуации, когданесколько потоков одновременно пытаются извлечь или добавить
элементы. Для этого используйте механизмы синхронизации (например,
мьютексы или семафоры), чтобы управлять доступом к очереди иизбежать состояния гонки данных.
